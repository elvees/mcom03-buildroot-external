config BR2_PACKAGE_POWERVR_ROGUE_KERN
	bool "powervr-rogue-kern"
	help
	  Kernel space driver for PowerVR Rogue GPU.

if BR2_PACKAGE_POWERVR_ROGUE_KERN
config BR2_PACKAGE_POWERVR_ROGUE_KERN_DEBUG
	bool "debug build"
	help
	  Useful for testing. If some tests fail this option
	  should be enabled.

config BR2_PACKAGE_POWERVR_ROGUE_KERN_PDUMP
	bool "pdump build"
	help
	  PDump is used for snapshoting and playing back the
	  applications.
endif
